<?php
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
// Simple healthcheck endpoint that doesn't require Laravel bootstrap
header('Content-Type: application/json');
http_response_code(200);
echo json_encode([
    'status' => 'ok',
    'timestamp' => date('c'),
    'server' => 'php'
]);
exit;
